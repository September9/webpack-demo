<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title><%= htmlWebpackPlugin.options.title || 'Webpack demo'%></title>
    </head>
    <body>
        <div id="<%= htmlWebpackPlugin.options.appMountId || 'root' %>"></div>
    </body>
    <% for (var chunk in htmlWebpackPlugin.files.chunks) { %>
        <script src="<%= htmlWebpackPlugin.files.chunks[chunk].entry %>"></script>
        <% } %>
</html>